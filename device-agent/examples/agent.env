# MuMu Camera Device Agent Environment Variables
# Place this file at /etc/mumucam/agent.env

# Device ID (required, must be unique for each device)
DEVICE_ID=pi-cam-001

# Backend WebSocket URL
# Local: ws://localhost:8000/ws/device
# Direct: ws://your-server-ip:8000/ws/device or wss://your-domain.com/ws/device
# Cloudflare Tunnel: wss://backend.your-domain.com/ws/device
BACKEND_URL=wss://backend.your-domain.com/ws/device

# go2rtc HTTP API URL (local)
GO2RTC_HTTP=http://127.0.0.1:1984

# Device Secret (optional, for authentication)
# Generate with: openssl rand -hex 32
DEVICE_SECRET=your-secret-key-here
